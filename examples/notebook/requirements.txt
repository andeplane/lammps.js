# JupyterLite site with the Python (Pyodide) kernel. The notebooks drive
# lammps.js through the bundled `lammps-js` Python bindings (repo-root
# python/), which mirror the official LAMMPS Python module.
jupyterlite-core==0.8.0
jupyterlite-pyodide-kernel==0.8.1
# Needed by `jupyter lite build --contents` to index the content directory.
jupyter-server~=2.19
# Builds the `lammps-js` Python bindings wheel that build.sh bundles into
# the site's pypi/ index.
build~=1.2
