{
  "name": "lammps-threejs-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "npm --prefix ../.. run build",
    "dev": "vite",
    "prebuild": "npm --prefix ../.. run build",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lammps.js": "file:../../",
    "three": "^0.168.0"
  },
  "devDependencies": {
    "@types/three": "^0.167.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
